# React Native 開發指南：組件比較與應用發佈

## 🎮 ScrollView vs FlatList

### 使用 FlatList 的情況：

- **效能考量**：FlatList 只渲染螢幕上當前可見的項目，節省記憶體並提高效能
- **大量數據列表**：當需要渲染可能包含大量數據的列表時（如動態消息、搜索結果、訊息列表）
- **未知內容長度**：當事先不知道需要顯示多少項目時
- **相同類型內容**：當顯示多個具有相同結構的項目時

### 使用 ScrollView 的情況：

- **內容量少且能放入記憶體**：顯示體積小、固定數量的內容，不會造成效能問題
- **靜態內容**：用於顯示預先確定且有限的內容，如表單、個人資料頁或詳情頁
- **混合內容類型**：當需要在特定布局中顯示不同UI組件且不遵循列表模式時
- **橫向輪播元素**：小型橫向滾動組件，如包含少量項目的圖片輪播

## 🚀 Pressable vs TouchableOpacity

### 使用 Pressable 的情況：

- **需要更多客製化**：Pressable 提供更多針對不同狀態（按壓、懸停、聚焦）的客製化選項
- **複雜互動狀態**：需要處理多種互動狀態且具備精細控制
- **面向未來**：Pressable 是較新的組件，設計用於最終取代 Touchable 系列組件
- **跨平台行為**：希望在不同平台自訂行為時
- **嵌套按壓處理器**：需要處理嵌套的互動元素時

### 使用 TouchableOpacity 的情況：

- **簡單淡入效果**：只需要按壓時的簡單透明度變化
- **向後兼容性**：處理已使用 TouchableOpacity 的舊有代碼庫
- **更簡單的 API**：喜歡更直接的 API，配置選項較少
- **特定透明度動畫**：需要對按壓時的透明度值進行精確控制
- **保持一致性**：為了與現有組件保持一致

## 📸 Expo Image vs React Native Image

### 使用 Expo Image 的情況：

- **效能**：Expo Image 使用原生圖像庫，提供更好的效能
- **快取**：內建的快取系統更穩健且可配置
- **現代圖像功能**：需要內容感知調整大小、模糊佔位符和漸進式加載等進階功能
- **轉場效果**：需要在圖像加載狀態之間平滑過渡
- **跨平台一致性**：在 iOS 和 Android 平台上行為更一致
- **適應性**：對不同螢幕尺寸和解析度的圖像有更好的支援

### 使用 React Native Image 的情況：

- **簡單性**：需要基本圖像顯示，配置最少
- **套件大小**：嘗試縮小應用程式的套件大小
- **無 Expo 依賴**：不使用 Expo 或希望最小化依賴
- **舊有支援**：維護使用 React Native Image 的現有代碼時的兼容性
- **基本需求**：不需要進階圖像功能時

## 🖼️ icon.png vs adaptive-icon.png

- **icon.png**：標準應用圖標，顯示在大多數設備上，建議尺寸 1024x1024
- **adaptive-icon.png**：Android 8.0 (Oreo) 引入的特定圖標，僅用於 Android 設備，建議尺寸 1024x1024

_如果不提供這些圖標，您的應用仍能運作，但會使用 Expo 的預設圖標。對於計劃發布到 App Store 或 Play Store 的專業應用，您應該包含自定義圖標。_

## 📦 構建與發佈

您可以使用 Expo Application Services (EAS) 為生產環境構建應用：

- 提交到 Google Play Store / App Store 需要開發者帳號
- 應用審核通過並上線可能需要幾天/幾週時間

### 步驟：

1. 訪問 expo.dev 並註冊
2. `npm i -g eas-cli`
3. `eas login`
4. `eas init`（會詢問是否創建項目，回答 yes）
5. `eas build --platform android`（構建 Android 版，將生成 APK 文件）
6. `eas build --platform ios`（構建 iOS 版，將生成 IPA 文件）
7. 將這些文件提交到應用商店
